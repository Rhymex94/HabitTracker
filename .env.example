# Environment Configuration Example
# Copy this file to .env and configure for your environment

# ============================================
# Environment Mode (REQUIRED for development)
# ============================================
# Set to 'development' to allow using the default SECRET_KEY
# Set to 'production' to enforce HTTPS and require SECRET_KEY
ENVIRONMENT=development

# ============================================
# Security (REQUIRED in production)
# ============================================
# Generate a secure key with: python -c 'import secrets; print(secrets.token_hex(32))'
# Uncomment and set this for production (or any persistent development environment):
# SECRET_KEY=your-generated-secret-key-here

# ============================================
# URLs Configuration (REQUIRED for docker-compose.host.yml)
# ============================================
# FRONTEND_ORIGIN: The URL of the frontend app (for CORS)
# VITE_API_URL: The URL of the backend API (used at frontend build time)
# Use https:// in production when HTTPS is enforced
FRONTEND_ORIGIN=http://localhost:5173
VITE_API_URL=http://localhost:5000

# ============================================
# MySQL Database Configuration (REQUIRED)
# ============================================
# These credentials are used by the MySQL container and must match DATABASE_URL
MYSQL_DATABASE=habits_db
MYSQL_USER=user
MYSQL_PASSWORD=password
MYSQL_ROOT_PASSWORD=rootpassword

# ============================================
# Database Connection URL
# ============================================
# Format: mysql+pymysql://<MYSQL_USER>:<MYSQL_PASSWORD>@db/<MYSQL_DATABASE>
DATABASE_URL=mysql+pymysql://user:password@db/habits_db

# ============================================
# Redis Configuration (optional)
# ============================================
# Used for caching streak calculations and rate limiting.
# If not set, caching is disabled and rate limiting uses in-memory storage.
REDIS_URL=redis://redis:6379/0

# ============================================
# Port Configuration (optional, docker-compose.host.yml only)
# ============================================
# Host-side ports for accessing services. Defaults shown below.
# Only change these if the default ports conflict with other services.
# BACKEND_PORT=8320
# FRONTEND_PORT=8321
# MYSQL_PORT=3306
# REDIS_PORT=6379
